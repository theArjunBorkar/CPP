#include <iostream>
using namespace std;
int main()
{
    int x,y;
    cout << "Enter 1st number: ";
    cin >> x;
    cout << "Enter 2nd number: ";
    cin >> y;
    (x > y) ? cout << "The larger number is " << x : cout << "The largest value is " << y;
     return 0;
}
